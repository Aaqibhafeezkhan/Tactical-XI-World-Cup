{
  "name": "tactical-xi-world-cup-2026",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepare:data": "node scripts/sync-world-cup-data.mjs && node scripts/prepare-historical-world-cup.mjs",
    "prepare:historical": "node scripts/prepare-historical-world-cup.mjs",
    "dev": "npm run prepare:data && vite",
    "build": "npm run prepare:data && tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest",
    "lucide-react": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "vitest": "latest",
    "jsdom": "latest"
  }
}
